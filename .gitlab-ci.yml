image: node:8.9

cache:
  paths:
  - node_modules/

pages:
  stage: deploy
  script:
  - npm install
  - node node_modules/docpad/bin/docpad generate -o public --env static
  artifacts:
    paths:
    - public
  only:
  - source

variables:
  GIT_SUBMODULE_STRATEGY: recursive
